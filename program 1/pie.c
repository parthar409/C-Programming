#include<stdio.h>
#define Pi 3.14
int main() { 
    //float radius;
    //printf("enter radius");
    //scanf("%f", &radius);
    float radius = 4, area;
    area = Pi * radius * radius;
    printf("area is : %f", area);
    return 0;
}